<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Book</title>

  <link rel="stylesheet" href="/edit.css">
</head>
<body>
  <header class="edit-header text-center">
    <h1>Edit Book</h1>
  </header>

  <main class="edit-main">
    <form action="/books/edit/<%= book.id %>" method="POST" class="edit-book-form">
      <div class="form-group">
        <label for="title">Title:</label>
        <input type="text" id="title" name="title" value="<%= book.title %>" required>
      </div>

      <div class="form-group">
        <label for="author">Author:</label>
        <input type="text" id="author" name="author" value="<%= book.author %>">
      </div>

      <div class="form-group">
        <label for="rating">Rating:</label>
        <input type="number" id="rating" name="rating" min="0" max="5" value="<%= book.rating %>">
      </div>

      <div class="form-group">
        <label for="date_read">Date Read:</label>
        <input type="date" id="date_read" name="date_read" value="<%= book.date_read ? book.date_read.toISOString().slice(0, 10) : '' %>">
      </div>

      <div class="form-group">
        <label for="notes">Notes:</label>
        <textarea id="notes" name="notes"><%= book.notes %></textarea>
      </div>

      <button type="submit" class="submit-btn">Update Book</button>
    </form>
    <a href="/" class="back-link">Back to Home</a>
  </main>
</body>
</html>
